---
phase: 02-definition-parameter-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [docs/index.html]
autonomous: false
requirements: [PARAM-01, PARAM-02, PARAM-03, PARAM-04]

must_haves:
  truths:
    - "Developer can find String, Int, Bool with their CUE type equivalents"
    - "Developer can find Array, List, StringList, StringKeyMap, Object with collection semantics and CUE equivalents"
    - "Developer can find Enum, Struct, OneOf with their CUE union/struct equivalents"
    - "Every chain method (Required, Optional, Default, Description, Short, Values, Min, Max, WithFields, Of, ForceOptional, Ignore) has a Go snippet and CUE equivalent"
    - "The parameter-builders section contains no placeholder-box — fully replaced with real content"
  artifacts:
    - path: "docs/index.html"
      provides: "Parameter Builders section with real HTML content"
      contains: "id='parameter-builders'"
  key_links:
    - from: "docs/index.html#parameter-builders"
      to: ".method-card entries for scalar, collection, complex types plus chain methods"
      via: "HTML section replacement"
      pattern: "pill-param"
---

<objective>
Inject real HTML content into the `parameter-builders` section of docs/index.html, replacing the Phase 2 placeholder with documented scalar types (String/Int/Bool), collection types (Array/List/StringList/StringKeyMap/Object), complex types (Enum/Struct/OneOf), and a dedicated chain methods reference with side-by-side Go+CUE examples.

Purpose: Delivers PARAM-01 through PARAM-04. Developers can look up any parameter constructor, see its CUE type mapping, and understand every available chain method with concrete examples.
Output: docs/index.html with the parameter-builders section fully populated.
</objective>

<execution_context>
@/Users/viskumar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viskumar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-page-shell/01-01-SUMMARY.md

<interfaces>
CSS classes available in docs/index.html (from Phase 1):
- .method-card, .method-header, .method-sig, .method-meta, .method-title, .method-desc, .method-body
- .method-applies + .applies-label + .pill .pill-param .pill-component .pill-trait .pill-all
- .code-pair (CSS grid 1fr 1fr), .code-block, .code-label.go, .code-label.cue
- pre.language-go / pre.language-cue for Prism.js syntax highlighting

Section anchor to target: id="parameter-builders"
Placeholder to REPLACE (entire div, including closing tag):
  <div class="placeholder-box">
    <div class="pl-icon">📋</div>
    <p>Full API reference for all parameter types ... coming in Phase 2.</p>
    <div class="pl-phase">Phase 2</div>
  </div>

CUE mapping reference (canonical, use exactly):
  String("x").Required()     → x: string
  String("x")                → x?: string
  String("x").Default("v")   → x: *"v" | string
  Int("x").Default(1)        → x: *1 | int
  Bool("x").Default(true)    → x: *true | bool
  Enum("x").Values("a","b")  → x: "a" | "b"
  StringList("x")            → x?: [...string]
  StringKeyMap("x")          → x?: [string]: string
  Array("x").Of(T)           → x?: [...T]
  Object("x")                → x?: {...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Parameter Builders HTML — all types and chain methods</name>
  <files>docs/index.html</files>
  <action>
Read docs/index.html. Locate the parameter-builders section (id="parameter-builders"). Find and REPLACE the entire placeholder-box div (lines ~953-957) with the full HTML content below.

The replacement documents PARAM-01 (scalar), PARAM-02 (collections), PARAM-03 (complex), and PARAM-04 (chain methods) in four logical groups, each with method-cards and code-pairs.

Structure to inject (replace the placeholder-box div with this exact block):

```html
<!-- ── SCALAR TYPES ───────────────────────── -->
<h3 style="font-size:1rem;font-weight:700;margin:0 0 1rem;color:var(--text);">Scalar Types</h3>

<!-- String -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.String(name string) *Param</code>
    <div class="method-meta">
      <div class="method-title">String</div>
      <div class="method-desc">A UTF-8 string parameter. Optional by default — use <code>.Required()</code> to enforce presence.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>image   := defkit.String("image").Required()
tag     := defkit.String("tag").Default("latest")
comment := defkit.String("comment") // optional, no default</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>image:    string
tag:      *"latest" | string
comment?: string</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Int -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.Int(name string) *Param</code>
    <div class="method-meta">
      <div class="method-title">Int</div>
      <div class="method-desc">A 64-bit integer parameter. Supports <code>.Min(n)</code> and <code>.Max(n)</code> for range constraints.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>replicas := defkit.Int("replicas").Default(1).Min(1).Max(50)
port     := defkit.Int("port").Required()</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>replicas: *1 | int &gt;=1 &amp; &lt;=50
port:     int</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Bool -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.Bool(name string) *Param</code>
    <div class="method-meta">
      <div class="method-title">Bool</div>
      <div class="method-desc">A boolean parameter. <code>.Default(true)</code> or <code>.Default(false)</code> sets the CUE default value.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>enable := defkit.Bool("enable").Default(true)
debug  := defkit.Bool("debug").Default(false)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>enable: *true  | bool
debug:  *false | bool</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- ── COLLECTION TYPES ────────────────────── -->
<h3 style="font-size:1rem;font-weight:700;margin:2rem 0 1rem;color:var(--text);">Collection Types</h3>

<!-- StringList -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.StringList(name string) *Param</code>
    <div class="method-meta">
      <div class="method-title">StringList</div>
      <div class="method-desc">A list of strings. Shorthand for <code>Array("x").Of(String)</code>. Optional by default.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>args    := defkit.StringList("args")
secrets := defkit.StringList("secrets").Required()</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>args?:   [...string]
secrets: [...string]</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Array -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.Array(name string).Of(paramType) *Param</code>
    <div class="method-meta">
      <div class="method-title">Array</div>
      <div class="method-desc">A typed list where every element matches the given parameter type. Chain <code>.Of()</code> with any scalar, struct, or complex type. Use when items have a defined schema.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>envItem := defkit.Struct("envItem").WithFields(
    defkit.String("name").Required(),
    defkit.String("value").Optional(),
)
envs := defkit.Array("env").Of(envItem)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>env?: [...{
    name:   string
    value?: string
}]</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- List -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.List(name string) *Param</code>
    <div class="method-meta">
      <div class="method-title">List</div>
      <div class="method-desc">An untyped list (CUE <code>[...]</code>). Use when items are heterogeneous or the schema is provided externally via <code>.WithSchema()</code>.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>objects := defkit.List("objects").Required()</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>objects: [...]</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Object -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.Object(name string).WithFields(params...) *Param</code>
    <div class="method-meta">
      <div class="method-title">Object</div>
      <div class="method-desc">A structured object with a known set of named fields. Use <code>.WithFields()</code> to specify the field parameter definitions. Prefer <code>Struct</code> when used inside <code>Array.Of()</code>.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>resources := defkit.Object("resources").WithFields(
    defkit.Object("limits").WithFields(
        defkit.String("cpu").Default("500m"),
        defkit.String("memory").Default("256Mi"),
    ),
    defkit.Object("requests").WithFields(
        defkit.String("cpu").Default("100m"),
        defkit.String("memory").Default("128Mi"),
    ),
)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>resources?: {
    limits?: {
        cpu:    *"500m"  | string
        memory: *"256Mi" | string
    }
    requests?: {
        cpu:    *"100m"  | string
        memory: *"128Mi" | string
    }
}</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- StringKeyMap -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.StringKeyMap(name string) *Param</code>
    <div class="method-meta">
      <div class="method-title">StringKeyMap</div>
      <div class="method-desc">A map with arbitrary string keys and string values. Maps directly to the CUE <code>[string]: string</code> constraint. Useful for labels, annotations, or environment variable maps.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>labels      := defkit.StringKeyMap("labels")
annotations := defkit.StringKeyMap("annotations")</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>labels?:      [string]: string
annotations?: [string]: string</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- ── COMPLEX TYPES ───────────────────────── -->
<h3 style="font-size:1rem;font-weight:700;margin:2rem 0 1rem;color:var(--text);">Complex Types</h3>

<!-- Enum -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.Enum(name string).Values(vals ...string) *Param</code>
    <div class="method-meta">
      <div class="method-title">Enum</div>
      <div class="method-desc">A string parameter constrained to a fixed set of allowed values. Use <code>.Values()</code> — not <code>.Enum()</code> — to specify the allowed set (updated API, current convention).</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>// Use .Values(), not .Enum() — current convention
pullPolicy := defkit.Enum("imagePullPolicy").
    Values("Always", "IfNotPresent", "Never").
    Default("IfNotPresent")</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>imagePullPolicy: *"IfNotPresent" | "Always" | "IfNotPresent" | "Never"</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Struct -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.Struct(name string).WithFields(params...) *Param</code>
    <div class="method-meta">
      <div class="method-title">Struct</div>
      <div class="method-desc">An inline struct type with named fields. Identical semantics to <code>Object</code> but conventionally used as the element type inside <code>Array.Of()</code>. Can nest other parameter types including other Structs.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>portStruct := defkit.Struct("port").WithFields(
    defkit.Int("containerPort").Required(),
    defkit.Enum("protocol").Values("TCP", "UDP").Default("TCP"),
    defkit.String("name").Optional(),
)
ports := defkit.Array("ports").Of(portStruct)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>ports?: [...{
    containerPort: int
    protocol:      *"TCP" | "TCP" | "UDP"
    name?:         string
}]</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- OneOf -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.OneOf(name string).Variants(params...) *Param</code>
    <div class="method-meta">
      <div class="method-title">OneOf</div>
      <div class="method-desc">A discriminated union — the parameter must match exactly one of the declared variant schemas. Each variant is defined with <code>defkit.Variant(name)</code>. Maps to a CUE disjunction of struct types.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies"><span class="applies-label">Applies to:</span><span class="pill pill-all">All Definition Types</span></div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>source := defkit.OneOf("source").Variants(
    defkit.Variant("git").WithFields(
        defkit.String("url").Required(),
        defkit.String("branch").Default("main"),
    ),
    defkit.Variant("image").WithFields(
        defkit.String("ref").Required(),
    ),
)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>source?: {
    url:    string
    branch: *"main" | string
} | {
    ref: string
}</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- ── CHAIN METHODS ───────────────────────── -->
<h3 style="font-size:1rem;font-weight:700;margin:2rem 0 1rem;color:var(--text);">Chain Methods</h3>
<p style="font-size:0.82rem;color:var(--text-muted);margin-bottom:1.25rem;">All parameter constructors return <code>*Param</code> — chain these methods to configure validation, defaults, and metadata. Methods are composable in any order.</p>

<!-- Required / Optional -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Required() / .Optional() / .ForceOptional()</code>
    <div class="method-meta">
      <div class="method-title">Required / Optional / ForceOptional</div>
      <div class="method-desc">Controls whether the parameter must be supplied. Parameters are optional (<code>x?</code> in CUE) by default. <code>.Required()</code> makes the field mandatory. <code>.ForceOptional()</code> overrides Required back to optional — useful for overriding inherited schemas.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>defkit.String("image").Required()        // mandatory
defkit.String("tag")                     // optional (default)
defkit.String("tag").Optional()          // explicit optional
defkit.String("name").ForceOptional()    // override to optional</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>image: string     // required — no ?
tag?:  string     // optional — has ?
name?: string     // force-optional — has ?</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Default -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Default(value interface{}) *Param</code>
    <div class="method-meta">
      <div class="method-title">Default</div>
      <div class="method-desc">Sets a default value. The CUE default marker (<code>*</code>) is placed before the value in the disjunction. Works with strings, ints, bools, and string slices.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>defkit.String("tag").Default("latest")
defkit.Int("replicas").Default(1)
defkit.Bool("readOnly").Default(false)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>tag:      *"latest" | string
replicas: *1        | int
readOnly: *false    | bool</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Description / Short -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Description(text string) / .Short(letter rune) *Param</code>
    <div class="method-meta">
      <div class="method-title">Description / Short</div>
      <div class="method-desc"><code>.Description()</code> adds a human-readable description shown in <code>vela show</code> and generated OpenAPI schemas. <code>.Short()</code> assigns a single-letter CLI shorthand flag.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>defkit.String("image").
    Required().
    Short('i').
    Description("Container image reference (e.g. nginx:1.25)")</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated annotation</div>
        <pre class="language-cue"><code>// +usage=Container image reference (e.g. nginx:1.25)
// +short=i
image: string</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Values -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Values(vals ...string) *Param</code>
    <div class="method-meta">
      <div class="method-title">Values</div>
      <div class="method-desc">Restricts an <code>Enum</code> parameter to the listed allowed values. Produces a CUE string disjunction. Combine with <code>.Default()</code> to set a pre-selected value.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>defkit.Enum("policy").
    Values("Retain", "Delete", "Recycle").
    Default("Retain")</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>policy: *"Retain" | "Retain" | "Delete" | "Recycle"</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Min / Max -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Min(n int) / .Max(n int) *Param</code>
    <div class="method-meta">
      <div class="method-title">Min / Max</div>
      <div class="method-desc">Applies inclusive range constraints to <code>Int</code> parameters. Generates CUE constraint expressions using <code>&gt;=</code> and <code>&lt;=</code> intersected with the base type.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>defkit.Int("replicas").Default(1).Min(1).Max(100)
defkit.Int("timeout").Default(30).Min(5)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>replicas: *1  | int &gt;=1 &amp; &lt;=100
timeout:  *30 | int &gt;=5</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- WithFields -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.WithFields(params ...*Param) *Param</code>
    <div class="method-meta">
      <div class="method-title">WithFields</div>
      <div class="method-desc">Defines the named fields of an <code>Object</code> or <code>Struct</code> parameter. Each field is itself a fully-configured <code>*Param</code>, enabling nested structures of arbitrary depth.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>probe := defkit.Object("livenessProbe").WithFields(
    defkit.String("path").Default("/healthz"),
    defkit.Int("port").Default(8080),
    defkit.Int("initialDelaySeconds").Default(10),
)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>livenessProbe?: {
    path:                *"/healthz" | string
    port:                *8080 | int
    initialDelaySeconds: *10   | int
}</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Of -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Of(paramType *Param) *Param</code>
    <div class="method-meta">
      <div class="method-title">Of</div>
      <div class="method-desc">Sets the element type of an <code>Array</code>. Pass any <code>*Param</code> — scalar, struct, or complex — as the element schema. The element type does not need a name when passed to <code>.Of()</code>; the array's name is used.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>// Array of strings
tags := defkit.Array("tags").Of(defkit.String(""))

// Array of structs
mounts := defkit.Array("volumeMounts").Of(
    defkit.Struct("").WithFields(
        defkit.String("name").Required(),
        defkit.String("mountPath").Required(),
        defkit.Bool("readOnly").Default(false),
    ),
)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>tags?: [...string]

volumeMounts?: [...{
    name:      string
    mountPath: string
    readOnly:  *false | bool
}]</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Ignore -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.Ignore() *Param</code>
    <div class="method-meta">
      <div class="method-title">Ignore</div>
      <div class="method-desc">Marks the parameter as internal — it is excluded from the generated CUE schema and will not appear in <code>vela show</code> or OpenAPI output. Use for helper variables that exist only in Go for template composition.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>// internal variable — not exposed in CUE schema
helper := defkit.String("_internalHelper").Ignore()

return defkit.NewComponent("my-comp").
    Params(image, helper).  // helper excluded from schema
    Template(myTemplate)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>// Only non-ignored params appear:
parameter: {
    image: string
    // _internalHelper is omitted
}</code></pre>
      </div>
    </div>
  </div>
</div>
```

Write this block replacing the entire `<div class="placeholder-box">...</div>` inside the `parameter-builders` section. Do NOT touch the section-header div, section-desc paragraph, or any other section. Pay careful attention to only replace the placeholder in the parameter-builders section — do NOT touch the definition-builders or any other section's content.
  </action>
  <verify>
    <automated>grep -c "pill-param\|pill-all" /Users/viskumar/Open_Source/vela-go-definitions/docs/index.html</automated>
  </verify>
  <done>docs/index.html has multiple pill-param or pill-all occurrences in the parameter-builders section; no placeholder-box remains in that section; the file remains valid HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Parameter Builders section in docs/index.html — 3 scalar type cards (String, Int, Bool), 5 collection type cards (StringList, Array, List, Object, StringKeyMap), 3 complex type cards (Enum, Struct, OneOf), and 8 chain method cards (Required/Optional/ForceOptional, Default, Description/Short, Values, Min/Max, WithFields, Of, Ignore). Each card has a side-by-side Go+CUE code pair.</what-built>
  <how-to-verify>
    1. Open docs/index.html in a browser
    2. Click "Scalar Types" in the sidebar — confirm it scrolls to the Parameter Builders section
    3. Verify three group headings: "Scalar Types", "Collection Types", "Complex Types", "Chain Methods"
    4. Verify String/Int/Bool cards each show correct CUE mapping (Required → no ?, Optional → ?, Default → *)
    5. Verify StringKeyMap card shows [string]: string CUE mapping
    6. Verify Enum card notes to use .Values() not .Enum()
    7. Verify chain methods section has cards for Required/Optional, Default, Description/Short, Values, Min/Max, WithFields, Of, Ignore
    8. Verify no placeholder box in the parameter-builders section
    9. Verify definition-builders section (already done in plan 02-01) is unaffected
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- grep "Scalar Types\|Collection Types\|Complex Types\|Chain Methods" docs/index.html returns 4 hits in parameter-builders section
- grep "StringKeyMap\|OneOf\|ForceOptional" docs/index.html returns hits
- grep -c "placeholder-box" docs/index.html is reduced (parameter-builders placeholder removed)
- Both definition-builders and parameter-builders sections have real content
</verification>

<success_criteria>
- All 11 parameter type cards present (3 scalar + 5 collection + 3 complex)
- All 8 chain method cards present
- CUE mappings are accurate per the canonical mapping table in context
- Enum card explicitly notes .Values() not .Enum() (current convention)
- parameter-builders section contains no placeholder-box
- Human visually approves the rendered page
- PARAM-01 through PARAM-04 are all covered
</success_criteria>

<output>
After completion, create `.planning/phases/02-definition-parameter-api/02-02-SUMMARY.md` using the standard summary template.
</output>
