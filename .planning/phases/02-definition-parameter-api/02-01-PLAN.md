---
phase: 02-definition-parameter-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/index.html]
autonomous: false
requirements: [DEF-01, DEF-02, DEF-03, DEF-04, DEF-05, DEF-06, DEF-07, DEF-08]

must_haves:
  truths:
    - "Developer can find NewComponent with signature, chain method table, and a side-by-side Go+CUE example"
    - "Developer can find NewTrait with AppliesTo/PodDisruptive/ConflictsWith chain methods and example"
    - "Developer can find NewPolicy with its minimal chain set and example"
    - "Developer can find NewWorkflowStep with Scope/Category chain methods and example"
    - "AutodetectWorkload() has a dedicated card explaining when to use it (variable workload type) with example"
    - "RawCUE() has a dedicated card showing raw CUE string injection bypassing the fluent builder"
    - "WithImports() is documented with CUE import path syntax example"
    - "The definition-builders section contains no placeholder-box — it is fully replaced with real content"
  artifacts:
    - path: "docs/index.html"
      provides: "Definition Builders section with real HTML content"
      contains: "id='definition-builders'"
  key_links:
    - from: "docs/index.html#definition-builders"
      to: ".method-card entries for NewComponent, NewTrait, NewPolicy, NewWorkflowStep"
      via: "HTML section replacement"
      pattern: "method-card"
---

<objective>
Inject real HTML content into the `definition-builders` section of docs/index.html, replacing the Phase 2 placeholder with fully documented NewComponent, NewTrait, NewPolicy, NewWorkflowStep constructors, their chain methods per type, and AutodetectWorkload/RawCUE/WithImports special methods.

Purpose: Delivers DEF-01 through DEF-08. Developers can look up any definition constructor, see its full chain method set, and see a side-by-side Go+CUE example.
Output: docs/index.html with the definition-builders section fully populated.
</objective>

<execution_context>
@/Users/viskumar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viskumar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-page-shell/01-01-SUMMARY.md

<!-- CSS classes available in docs/index.html (extracted from Phase 1 output) -->
<interfaces>
CSS classes for content authoring:
- .method-card: card container (background: var(--surface), border-radius:12px, border:1px solid var(--border))
- .method-header: top bar of card with .method-sig, .method-meta (.method-title, .method-desc)
- .method-sig: monospace code badge (color: #93c5fd)
- .method-body: content area below header (padding: 1.25rem)
- .method-applies: flex row for pills, preceded by .applies-label
- .pill + .pill-component/.pill-trait/.pill-policy/.pill-workflow/.pill-all: colored type badges
- .code-pair: CSS grid 1fr 1fr with 0.75rem gap — put two .code-block divs inside
- .code-block: card for one code panel (border-radius:9px, border:1px solid var(--border))
- .code-label.go / .code-label.cue: header strip above pre block (includes <span class="dot"></span>label text)
- pre.language-go / pre.language-cue: Prism.js highlighted code (use &lt; and &gt; for angle brackets)
- .section-desc: muted lead paragraph already present in the section — do NOT duplicate it

Section anchor to target: id="definition-builders"
Placeholder to REPLACE (entire div, including closing tag):
  <div class="placeholder-box">
    <div class="pl-icon">📋</div>
    <p>Full API reference for <strong>NewComponent</strong> ... coming in Phase 2.</p>
    <div class="pl-phase">Phase 2</div>
  </div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Definition Builders HTML — constructors and chain methods</name>
  <files>docs/index.html</files>
  <action>
Read docs/index.html. Locate the definition-builders section. Find and REPLACE the entire placeholder-box div (lines ~937-941) with the full HTML content below.

The replacement content documents all four constructors (DEF-01 through DEF-04), their chain methods (DEF-05), and AutodetectWorkload / RawCUE / WithImports (DEF-06, DEF-07, DEF-08).

Structure to inject (replace the placeholder-box div with this exact block):

```html
<!-- NewComponent -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.NewComponent(name string) *ComponentDefinition</code>
    <div class="method-meta">
      <div class="method-title">NewComponent</div>
      <div class="method-desc">Creates a ComponentDefinition builder. Returns a fluent chain to configure the workload type, parameters, and CUE template function.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Type:</span>
      <span class="pill pill-component">Component</span>
    </div>
    <p style="font-size:0.82rem;color:var(--text-muted);margin-bottom:0.75rem;">Chain methods available on <code>*ComponentDefinition</code>:</p>
    <table style="width:100%;border-collapse:collapse;font-size:0.8rem;margin-bottom:1rem;">
      <thead><tr style="border-bottom:1px solid var(--border);">
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Method</th>
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Description</th>
      </tr></thead>
      <tbody>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.Description(text)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Human-readable description shown in <code>vela show</code>.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.Workload(apiVersion, kind)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Sets the fixed Kubernetes workload GVK. Use when the workload type is always the same (e.g., apps/v1, Deployment).</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.AutodetectWorkload()</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Omits workload GVK — KubeVela detects it from the template output. Use for k8s-objects, ref-objects.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.Params(params...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers one or more parameter definitions built with the parameter builder functions.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.Template(func(tpl))</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Assigns the CUE template function that builds runtime Kubernetes resources.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.Helper(name, param)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers a helper variable available inside the template function.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.CustomStatus(status)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Attaches a custom status DSL expression to the definition.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.HealthPolicy(health)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Attaches a health evaluation DSL expression. Use preset builders like <code>DeploymentHealth()</code>.</td></tr>
        <tr><td style="padding:0.35rem 0.5rem;"><code style="color:#93c5fd;">.WithImports(imports...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Adds CUE package import paths required by the template body.</td></tr>
      </tbody>
    </table>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>func Webservice() *defkit.ComponentDefinition {
    image := defkit.String("image").Required().
        Description("Container image to run")
    replicas := defkit.Int("replicas").Default(1).
        Description("Number of replicas")

    return defkit.NewComponent("webservice").
        Description("HTTP microservice backed by a Deployment").
        Workload("apps/v1", "Deployment").
        Params(image, replicas).
        Template(webserviceTemplate)
}

func init() { defkit.Register(Webservice()) }</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>// KubeVela generates this schema from the builder:
parameter: {
    image:    string
    replicas: *1 | int
}

// and the template function becomes the CUE
// output/patch body compiled by the controller</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- NewTrait -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.NewTrait(name string) *TraitDefinition</code>
    <div class="method-meta">
      <div class="method-title">NewTrait</div>
      <div class="method-desc">Creates a TraitDefinition builder. Traits patch or augment existing component workloads. They can target specific workload types and optionally mutate container specs.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Type:</span>
      <span class="pill pill-trait">Trait</span>
    </div>
    <p style="font-size:0.82rem;color:var(--text-muted);margin-bottom:0.75rem;">Chain methods available on <code>*TraitDefinition</code>:</p>
    <table style="width:100%;border-collapse:collapse;font-size:0.8rem;margin-bottom:1rem;">
      <thead><tr style="border-bottom:1px solid var(--border);">
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Method</th>
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Description</th>
      </tr></thead>
      <tbody>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.Description(text)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Human-readable description of the trait's purpose.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.AppliesTo(workloads...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Restricts which component types this trait can attach to (e.g., <code>"webservice"</code>, <code>"daemon"</code>). Omit for universal traits.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.ConflictsWith(traits...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Declares mutual exclusion with other named traits.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.PodDisruptive(bool)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Marks the trait as pod-disruptive (triggers pod restart when changed).</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.Stage(stage)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Sets the dispatch stage for ordering trait application.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.Labels(map)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Attaches metadata labels to the TraitDefinition CRD.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.Params(params...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers typed parameter definitions.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.Template(func(tpl))</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Assigns the patch template function. For container mutation use <code>tpl.UsePatchContainer(config)</code> inside.</td></tr>
        <tr><td style="padding:0.35rem 0.5rem;"><code style="color:#fb923c;">.Helper(name, param)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers a helper variable for use in the template function.</td></tr>
      </tbody>
    </table>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>func Env() *defkit.TraitDefinition {
    envs := defkit.Array("env").Of(defkit.Struct("envItem").
        WithFields(
            defkit.String("name").Required(),
            defkit.String("value").Optional(),
        )).Description("Environment variables to inject")

    return defkit.NewTrait("env").
        Description("Inject environment variables into containers").
        AppliesTo("webservice", "daemon", "worker").
        PodDisruptive(true).
        Params(envs).
        Template(envTemplate)
}

func init() { defkit.Register(Env()) }</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>parameter: {
    env?: [...{
        name:   string
        value?: string
    }]
}</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- NewPolicy -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.NewPolicy(name string) *PolicyDefinition</code>
    <div class="method-meta">
      <div class="method-title">NewPolicy</div>
      <div class="method-desc">Creates a PolicyDefinition builder. Policies are applied globally to applications and have no workload coupling. They control lifecycle behaviors like garbage collection, apply-once semantics, or override rules.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Type:</span>
      <span class="pill pill-policy">Policy</span>
    </div>
    <p style="font-size:0.82rem;color:var(--text-muted);margin-bottom:0.75rem;">Chain methods available on <code>*PolicyDefinition</code>:</p>
    <table style="width:100%;border-collapse:collapse;font-size:0.8rem;margin-bottom:1rem;">
      <thead><tr style="border-bottom:1px solid var(--border);">
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Method</th>
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Description</th>
      </tr></thead>
      <tbody>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#f472b6;">.Description(text)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Human-readable description of the policy behavior.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#f472b6;">.Params(params...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers typed parameter definitions for the policy.</td></tr>
        <tr><td style="padding:0.35rem 0.5rem;"><code style="color:#f472b6;">.Helper(name, param)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers a named helper variable for the template body.</td></tr>
      </tbody>
    </table>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>func ApplyOnce() *defkit.PolicyDefinition {
    enable := defkit.Bool("enable").Default(true).
        Description("Disable KubeVela from re-applying components on every reconcile")
    rules := defkit.Array("rules").Of(defkit.Struct("rule").
        WithFields(
            defkit.StringList("selector").Optional(),
        )).Optional()

    return defkit.NewPolicy("apply-once").
        Description("Prevent KubeVela from re-applying resources that already exist").
        Params(enable, rules)
}

func init() { defkit.Register(ApplyOnce()) }</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>parameter: {
    enable: *true | bool
    rules?: [...{
        selector?: [...string]
    }]
}</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- NewWorkflowStep -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">defkit.NewWorkflowStep(name string) *WorkflowStepDefinition</code>
    <div class="method-meta">
      <div class="method-title">NewWorkflowStep</div>
      <div class="method-desc">Creates a WorkflowStepDefinition builder. Workflow steps describe actions in application delivery pipelines — deploying components, running builds, applying Terraform configs, or checking metrics.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Type:</span>
      <span class="pill pill-workflow">WorkflowStep</span>
    </div>
    <p style="font-size:0.82rem;color:var(--text-muted);margin-bottom:0.75rem;">Chain methods available on <code>*WorkflowStepDefinition</code>:</p>
    <table style="width:100%;border-collapse:collapse;font-size:0.8rem;margin-bottom:1rem;">
      <thead><tr style="border-bottom:1px solid var(--border);">
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Method</th>
        <th style="text-align:left;padding:0.35rem 0.5rem;color:var(--text-muted);font-weight:600;">Description</th>
      </tr></thead>
      <tbody>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#22d3ee;">.Description(text)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Human-readable description of what the step does.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#22d3ee;">.Category(name)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Groups the step under a category in the KubeVela UI and CLI.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#22d3ee;">.Scope(scope)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Sets execution scope. Typically <code>"Application"</code> for delivery steps.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#22d3ee;">.Params(params...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Registers typed parameter definitions for the step.</td></tr>
        <tr style="border-bottom:1px solid var(--border-subtle);"><td style="padding:0.35rem 0.5rem;"><code style="color:#22d3ee;">.Template(func(tpl))</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Assigns the CUE template function executed when the step runs.</td></tr>
        <tr><td style="padding:0.35rem 0.5rem;"><code style="color:#22d3ee;">.WithImports(imports...)</code></td><td style="padding:0.35rem 0.5rem;color:var(--text-muted);">Adds CUE package import paths used by the template body.</td></tr>
      </tbody>
    </table>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>func ApplyComponent() *defkit.WorkflowStepDefinition {
    comp := defkit.String("component").Required().
        Description("Name of the component to apply")

    return defkit.NewWorkflowStep("apply-component").
        Description("Apply a single component from the Application").
        Category("Deploy").
        Scope("Application").
        Params(comp).
        Template(applyComponentTemplate)
}

func init() { defkit.Register(ApplyComponent()) }</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated</div>
        <pre class="language-cue"><code>parameter: {
    component: string
}</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- Special methods subsection -->
<h3 style="font-size:1rem;font-weight:700;margin:2rem 0 1rem;color:var(--text);">Special Methods</h3>

<!-- AutodetectWorkload -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.AutodetectWorkload() *ComponentDefinition</code>
    <div class="method-meta">
      <div class="method-title">AutodetectWorkload</div>
      <div class="method-desc">Omits a fixed workload GVK. Use when the component template can produce different resource kinds depending on parameters (e.g., k8s-objects accepts arbitrary manifests, ref-objects references existing resources).</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Applies to:</span>
      <span class="pill pill-component">Component</span>
    </div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>// Use AutodetectWorkload when the output kind varies
return defkit.NewComponent("k8s-objects").
    Description("Deploy arbitrary Kubernetes resources").
    AutodetectWorkload().   // no fixed GVK — KubeVela detects
    Params(objects).
    Template(k8sObjectsTemplate)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>When to use vs .Workload()</div>
        <pre class="language-cue"><code>// .Workload("apps/v1", "Deployment")
// → use when the component always produces
//   exactly one fixed resource kind

// .AutodetectWorkload()
// → use when template may output multiple
//   different resource kinds, or zero
//   (k8s-objects, ref-objects patterns)</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- RawCUE -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.RawCUE(cueTemplate string) *ComponentDefinition</code>
    <div class="method-meta">
      <div class="method-title">RawCUE</div>
      <div class="method-desc">Injects a raw CUE string as the template body, bypassing the fluent resource builder entirely. Use when the CUE logic cannot be expressed with the builder DSL (e.g., highly dynamic object iteration). Mutually exclusive with <code>.Template()</code>.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Applies to:</span>
      <span class="pill pill-component">Component</span>
    </div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>rawBody := `
    outputs: {
        for i, obj in parameter.objects {
            "object-\(i)": obj
        }
    }
`

return defkit.NewComponent("k8s-objects").
    AutodetectWorkload().
    Params(objects).
    RawCUE(rawBody)   // bypasses .Template()</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>Raw CUE body injected verbatim</div>
        <pre class="language-cue"><code>// The string is placed directly into the
// generated CUE definition file.
// You get full CUE power but lose the
// type-safe builder DSL.
//
// Use RawCUE only when the fluent builder
// cannot express what you need.</code></pre>
      </div>
    </div>
  </div>
</div>

<!-- WithImports -->
<div class="method-card">
  <div class="method-header">
    <code class="method-sig">.WithImports(imports ...string) *ComponentDefinition</code>
    <div class="method-meta">
      <div class="method-title">WithImports</div>
      <div class="method-desc">Adds CUE package import paths to the generated definition file. Required when your template body references CUE standard library packages (e.g., <code>strings</code>, <code>math</code>) or custom KubeVela packages. Available on ComponentDefinition and WorkflowStepDefinition.</div>
    </div>
  </div>
  <div class="method-body">
    <div class="method-applies">
      <span class="applies-label">Applies to:</span>
      <span class="pill pill-component">Component</span>
      <span class="pill pill-workflow">WorkflowStep</span>
    </div>
    <div class="code-pair">
      <div class="code-block">
        <div class="code-label go"><span class="dot"></span>Go — defkit</div>
        <pre class="language-go"><code>return defkit.NewComponent("my-component").
    Description("Uses CUE strings package").
    Workload("apps/v1", "Deployment").
    WithImports("strings").  // adds: import "strings"
    Params(name).
    Template(myTemplate)</code></pre>
      </div>
      <div class="code-block">
        <div class="code-label cue"><span class="dot"></span>CUE — generated header</div>
        <pre class="language-cue"><code>import (
    "strings"
)

// template body can now use:
//   strings.ToLower(parameter.name)</code></pre>
      </div>
    </div>
  </div>
</div>
```

Write this block replacing the entire `<div class="placeholder-box">...</div>` inside the `definition-builders` section. Do NOT touch the section-header div, section-desc paragraph, or any other section.
  </action>
  <verify>
    <automated>grep -c "method-card" /Users/viskumar/Open_Source/vela-go-definitions/docs/index.html</automated>
  </verify>
  <done>docs/index.html has 7 or more method-card divs inside the definition-builders section; the placeholder-box div is no longer present in that section; the file is valid HTML that opens without parse errors in a browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Definition Builders section in docs/index.html — 4 constructor cards (NewComponent, NewTrait, NewPolicy, NewWorkflowStep) each with a chain method table and side-by-side Go+CUE code pair, plus 3 special method cards (AutodetectWorkload, RawCUE, WithImports).</what-built>
  <how-to-verify>
    1. Open docs/index.html in a browser (file:// or any local server)
    2. Click "NewComponent" in the left sidebar — confirm it scrolls to the Definition Builders section
    3. Verify all 4 constructor cards are visible with method tables and code pairs
    4. Verify Go code blocks have blue syntax highlighting, CUE blocks have green
    5. Verify the three special method cards (AutodetectWorkload, RawCUE, WithImports) appear below the constructors under the "Special Methods" heading
    6. Verify no placeholder box appears in the definition-builders section
    7. Verify other sections (parameter-builders, template-methods, etc.) are unaffected
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/content issues to fix</resume-signal>
</task>

</tasks>

<verification>
- grep -c "placeholder-box" docs/index.html returns fewer occurrences than before (definition-builders placeholder removed)
- grep "NewComponent\|NewTrait\|NewPolicy\|NewWorkflowStep" docs/index.html returns hits
- grep "AutodetectWorkload\|RawCUE\|WithImports" docs/index.html returns hits
- The HTML file is well-formed (no unclosed tags introduced)
</verification>

<success_criteria>
- All 4 definition constructor cards present with chain method tables
- AutodetectWorkload, RawCUE, WithImports each have their own method-card with Go+CUE code-pair
- definition-builders section contains no placeholder-box
- Human visually approves the rendered page
- DEF-01 through DEF-08 are all covered
</success_criteria>

<output>
After completion, create `.planning/phases/02-definition-parameter-api/02-01-SUMMARY.md` using the standard summary template.
</output>
